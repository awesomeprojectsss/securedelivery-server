# Copy this file to `.env` and replace the placeholder with local credentials.
# Never commit the real value. SecureDelivery requires PostgreSQL >= 15.
DATABASE_URL="postgresql://securedelivery:securedelivery@localhost:5432/securedelivery"

# Generate a unique secret for each environment with: openssl rand -base64 32
AUTH_ACCESS_TOKEN_SECRET=""
